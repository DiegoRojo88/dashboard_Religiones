<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Proyecto — Dashboard</title>
  <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;1,400&family=DM+Sans:wght@300;400&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --paper:    #f5f0e8;
      --paper2:   #ede8dc;
      --ink:      #2a1f0e;
      --ink2:     #5a4a30;
      --muted:    #9a8a6a;
      --rule:     #d8cdb8;
      --accent:   #8b5e3c;
    }

    body {
      background: var(--paper);
      color: var(--ink);
      font-family: 'DM Sans', sans-serif;
      font-weight: 300;
      min-height: 100vh;
    }

    /* ── GRAIN TEXTURE ── */
    body::before {
      content: '';
      position: fixed;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='0.04'/%3E%3C/svg%3E");
      pointer-events: none;
      z-index: 0;
      opacity: .6;
    }

    /* ── LAYOUT ── */
    main {
      position: relative;
      z-index: 1;
      max-width: 900px;
      margin: 0 auto;
      padding: 72px 48px 80px;
    }

    /* ── MASTHEAD ── */
    .masthead {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      padding-bottom: 28px;
      border-bottom: 1.5px solid var(--ink);
      margin-bottom: 36px;
    }

    .masthead-left {}

    .eyebrow {
      font-size: 10px;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 10px;
    }

    h1 {
      font-family: 'Lora', serif;
      font-size: clamp(2rem, 5vw, 3.2rem);
      font-weight: 400;
      line-height: 1.1;
      letter-spacing: -.01em;
      color: var(--ink);
    }

    h1 em {
      font-style: italic;
      color: var(--accent);
    }

    .masthead-right {
      text-align: right;
      padding-top: 4px;
    }

    .team-label {
      font-size: 10px;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: var(--muted);
      margin-bottom: 8px;
    }

    .team-list {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 4px;
      align-items: flex-end;
    }

    .team-list li {
      font-family: 'Lora', serif;
      font-size: 13px;
      color: var(--ink2);
    }

    /* ── DESCRIPTION ── */
    .desc {
      font-size: 15px;
      color: var(--ink2);
      line-height: 1.75;
      max-width: 520px;
      margin-bottom: 48px;
      padding-left: 16px;
      border-left: 2px solid var(--rule);
    }

    /* ── RULE ── */
    .rule {
      display: flex;
      align-items: center;
      gap: 16px;
      margin-bottom: 32px;
    }

    .rule-line {
      flex: 1;
      height: 1px;
      background: var(--rule);
    }

    .rule-label {
      font-size: 10px;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: var(--muted);
      white-space: nowrap;
    }

    /* ── DASHBOARD ── */
    .dash-meta {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 14px;
      flex-wrap: wrap;
      gap: 10px;
    }

    .dash-title {
      font-family: 'Lora', serif;
      font-size: 16px;
      color: var(--ink);
      font-weight: 400;
    }

    .open-btn {
      font-size: 11px;
      letter-spacing: .06em;
      text-transform: uppercase;
      color: var(--accent);
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 6px 16px;
      border: 1px solid var(--accent);
      border-radius: 2px;
      transition: background .2s, color .2s;
    }
    .open-btn:hover {
      background: var(--accent);
      color: var(--paper);
    }
    .open-btn svg { width: 11px; height: 11px; }

    .iframe-wrap {
      border: 1px solid var(--rule);
      background: var(--paper2);
      overflow: hidden;
    }

    iframe {
      display: block;
      width: 100%;
      height: 580px;
      border: none;
    }

    /* ── FOOTER ── */
    footer {
      margin-top: 48px;
      padding-top: 20px;
      border-top: 1px solid var(--rule);
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 8px;
      font-size: 11px;
      letter-spacing: .06em;
      color: var(--muted);
      text-transform: uppercase;
    }

    /* ── RESPONSIVE ── */
    @media (max-width: 620px) {
      main { padding: 40px 20px 60px; }
      .masthead { flex-direction: column; gap: 24px; }
      .masthead-right { text-align: left; }
      .team-list { align-items: flex-start; }
      iframe { height: 380px; }
    }
  </style>
</head>
<body>
<main>

  <!-- MASTHEAD -->
  <div class="masthead">
    <div class="masthead-left">
      <p class="eyebrow">Tecnológico de Monterrey · Campus Querétaro</p>
      <h1>Proyecto <em>Dashboard</em></h1>
    </div>
    <div class="masthead-right">
      <p class="team-label">Equipo</p>
      <ul class="team-list">
        <!-- Cambia los nombres aquí -->
        <li>Miembro 1</li>
        <li>Miembro 2</li>
        <li>Miembro 3</li>
        <li>Miembro 4</li>
      </ul>
    </div>
  </div>

  <!-- DESCRIPCIÓN -->
  <p class="desc">
    Visualización interactiva de los datos del proyecto. Explora los indicadores clave directamente desde el dashboard embebido.
  </p>

  <!-- SEPARADOR -->
  <div class="rule">
    <div class="rule-line"></div>
    <span class="rule-label">Dashboard interactivo</span>
    <div class="rule-line"></div>
  </div>

  <!-- META + IFRAME -->
  <div class="dash-meta">
    <span class="dash-title">Análisis de datos</span>
    <a class="open-btn"
       href="https://app.powerbi.com/view?r=eyJrIjoiY2VjNDgwMmEtZGE4OS00M2MxLTk2NjItYWI5ZjgzMjY1ODVhIiwidCI6ImM2NWEzZWE2LTBmN2MtNDAwYi04OTM0LTVhNmRjMTcwNTY0NSIsImMiOjR9"
       target="_blank" rel="noopener">
      <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
        <path d="M7 2H2v12h12V9M9 2h5v5M14 2L8 8"/>
      </svg>
      Abrir en Power BI
    </a>
  </div>

  <div class="iframe-wrap">
    <iframe
      title="Dashboard Proyecto"
      src="https://app.powerbi.com/view?r=eyJrIjoiY2VjNDgwMmEtZGE4OS00M2MxLTk2NjItYWI5ZjgzMjY1ODVhIiwidCI6ImM2NWEzZWE2LTBmN2MtNDAwYi04OTM0LTVhNmRjMTcwNTY0NSIsImMiOjR9"
      allowFullScreen="true">
    </iframe>
  </div>

  <!-- FOOTER -->
  <footer>
    <span>Enero – Junio 2026</span>
    <span>MA · Optimización determinista</span>
  </footer>

</main>
</body>
</html>
